# PicHost 国际化 (i18n) 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 PicHost 实现 en/zh-CN 国际化:前端 ~350 条硬编码字符串接入 i18next,后端错误消息改为配置驱动的 i18n 模块(按语言分目录 TOML 文件 + 错误码 + 每请求 Accept-Language 协商 + 热更新)。

**Architecture:** 前端 i18next v26 + react-i18next + browser-languagedetector(`pichost-locale` localStorage,类型安全键);后端 pichost-core 新增 `src/i18n.rs`(`Language` + `I18n::t(locale, key, args)` + 回退链 + 全局单例热更新),消息目录 `locales/{en,zh-CN}/messages.toml`(内置 include_str! + 外部 `PICHOST_I18N_LOCALES_DIR` 覆盖),错误信封统一 `{"error": 本地化消息, "code": key}`,`Locale` 提取器按 Accept-Language → 部署配置 → en 解析。

**Tech Stack:** Rust (Axum 0.8, figment, toml), React 19 + Vite 8 + TypeScript 7, i18next + react-i18next + i18next-browser-languagedetector, vitest, Tailwind CSS 4。

## Global Constraints

- Rust 函数 ≤50 行,行宽 ≤120 字符(违反则用 rust-refactor-fns 拆分)。
- 质量门(每个任务结束): `cargo clippy --workspace -- -D warnings` + `cargo test --workspace`;前端任务另加 `npx vitest run` + `npm run build`。
- **en 目录消息值必须与现有代码中的英文字面量逐字一致**(保证默认部署响应不变、现有测试不破坏)。
- **每个抽取任务同步更新 `web-ui/src/i18n/locales/en.json` 与 `zh-CN.json`,并在 `src/i18n/i18n.test.ts` 维护键一致性断言**(en/zh 键集相等)。该文件为全局维护的测试文件,不在每个任务 files 中重复列出。
- 后端集成测试需 Docker PG+Redis(项目惯例 `#[ignore]`-gated,用 `-- --include-ignored` 运行)。
- 错误消息 key 命名 `{domain}_{reason}` snake_case,key 即错误信封的 `code` 字段。
- 语言协商: `Accept-Language` 优先 → 部署配置 `i18n.language`(默认 en)→ en。
- 热更新: 外部消息文件惰性 mtime 检查(节流 ≥5s);配置写入后显式 `I18n::reload_global()`。
- 提交信息英文,语义化;完成后版本 **0.17.5 → 0.18.0**(minor,功能特性)。

---

## 任务总览

| ID | 层 | 内容 | breaking |
|----|----|------|----------|
| T0 | core | config.rs 新增 I18nConfig(language/locales_dir) | no |
| T1 | core | i18n.rs 模块核心(Language/t/回退链)+ `toml` 依赖 | no |
| T2 | core | 内置消息目录 + `I18n::load()` 嵌入加载 | no |
| T3 | core | 全局单例 + reload + 惰性 mtime 热检查 | no |
| T4 | api | Locale 提取器 + error_json helper | no |
| T5 | core+api | AppError 信封加 code + JsonBody rejection 本地化 | yes |
| T6 | api | auth.rs 转换(auth.*/invite.* 键) | yes |
| T7 | api | images.rs 转换(image.* 键) | yes |
| T8 | api | upload.rs + upload_url.rs 转换 | yes |
| T9 | api | users.rs + categories.rs 转换 | yes |
| T10 | api | oauth.rs + middleware(auth/rate_limit)转换 | yes |
| T11 | api | admin.rs + storage_configs.rs 转换 | yes |
| T12 | api | config 服务 i18n 读写 + 启动 init_global + PUT 后 reload | no |
| F1 | web | i18n 引擎 + 依赖 + i18n.test.ts 基础 | no |
| F2 | web | 初始目录(common.*/nav.*)+ 键一致性测试 | no |
| F3 | web | 接线: index.html FOUC + main.tsx + 类型增强 | no |
| F4 | web | lib/format.ts + useFormat + 测试 | no |
| F5 | web | LanguageSwitcher + Login/Register 抽取 | no |
| F6 | web | NavBar/SearchBar/SortDropdown 抽取 | no |
| F7 | web | Dashboard/DropZone/UploadCard 抽取 | no |
| F8 | web | Gallery/UrlUploadInput/LinkCard 抽取 | no |
| F9 | web | ImageDetail/Settings 抽取 | no |
| F10 | web | CategoryTree/StorageConfigSection 抽取 | no |
| F11 | web | SystemConfig/Watermark/Preprocessing 抽取 | no |
| F12 | web | AdminStats/AdminUsers/EditUserDialog 抽取 | no |
| F13 | web | AdminInvites/CreateInviteDialog/Admin 抽取 | no |
| F14 | web | client.ts ApiError + Accept-Language hook + 行为码 | no |
| F15 | web | SystemConfig UI Language 字段 | no |
| F16 | 项目 | 版本 0.18.0 + AGENTS.md/README/summary 同步 | no |

---

## 后端任务

### Task T0: Add I18nConfig to core config

**Files:**
- Modify: `pichost-core/src/config.rs`(含 `#[cfg(test)] mod tests`)

**Interfaces:**
- Produces: `AppConfig.i18n: I18nConfig`;`I18nConfig { pub language: String, pub locales_dir: Option<PathBuf> }`,`Default` → `language: "en"`, `locales_dir: None`。字段带 `#[serde(default)]`。

**test_code**(先写,验证失败):
```rust
#[test]
fn test_defaults_i18n_config() {
    let cfg = AppConfig::default();
    assert_eq!(cfg.i18n.language, "en");
    assert!(cfg.i18n.locales_dir.is_none());
}
```
Run: `cargo test -p pichost-core test_defaults_i18n_config -- --exact` — Expected: FAIL(编译错误,`i18n` 字段不存在)。

**impl_code**(最小实现):
```rust
#[derive(Debug, Clone, Deserialize, Serialize)]
pub struct I18nConfig {
    #[serde(default = "default_i18n_language")]
    pub language: String,
    #[serde(default)]
    pub locales_dir: Option<PathBuf>,
}
fn default_i18n_language() -> String { "en".into() }
impl Default for I18nConfig {
    fn default() -> Self { Self { language: default_i18n_language(), locales_dir: None } }
}
// AppConfig 增加字段(带 #[serde(default)]),Default impl 补 i18n: I18nConfig::default()
```

**ac:**
- given: `AppConfig::default()` 创建后
- when: 读取 `cfg.i18n`
- then: `language == "en"` 且 `locales_dir.is_none()`;`load_config()` 无 env 时同样生效

**regression:**
- `cargo test -p pichost-core test_defaults_app_config -- --exact`

**verify:**
- `cargo test -p pichost-core test_defaults_i18n_config -- --exact`
- `cargo test -p pichost-core`
- `cargo clippy --workspace -- -D warnings`

---

### Task T1: Add core i18n module (Language, lookup, fallback)

**Files:**
- Modify: `pichost-core/Cargo.toml`(加 `toml = "0.8"`)
- Modify: `pichost-core/src/lib.rs`(加 `pub mod i18n;`)
- Create: `pichost-core/src/i18n.rs`(含 `#[cfg(test)] mod tests`)

**Interfaces:**
- Produces:
```rust
pub enum Language { En, ZhCN }
impl Language {
    pub fn from_str_opt(s: &str) -> Language;  // "zh"|"zh-CN"|"zh-cn" → ZhCN,其他 → warn+En
    pub fn as_str(self) -> &'static str;       // "en" / "zh-CN"
}
pub struct I18n { language: Language, messages: HashMap<Language, HashMap<String, String>> }
impl I18n {
    pub fn from_maps(language: Language, maps: HashMap<Language, HashMap<String, String>>) -> I18n;
    pub fn t(&self, locale: Language, key: &str) -> String;           // 回退链: locale → En → key 本身
    pub fn t_args(&self, locale: Language, key: &str, args: &[String]) -> String; // 顺序替换 "{}"
    pub fn language(&self) -> Language;
}
```

**test_code**(先写,验证失败):
```rust
fn maps() -> HashMap<Language, HashMap<String, String>> {
    let mut en = HashMap::new(); en.insert("greet".into(), "hello".into());
    let mut zh = HashMap::new(); zh.insert("greet".into(), "你好".into());
    HashMap::from([(Language::En, en), (Language::ZhCN, zh)])
}
#[test]
fn t_resolves_per_locale() {
    let i18n = I18n::from_maps(Language::En, maps());
    assert_eq!(i18n.t(Language::ZhCN, "greet"), "你好");
    assert_eq!(i18n.t(Language::En, "greet"), "hello");
}
#[test]
fn t_falls_back_zh_to_en_to_key() {
    let i18n = I18n::from_maps(Language::En, maps());
    assert_eq!(i18n.t(Language::ZhCN, "missing"), "missing");
    let mut en_only = HashMap::new(); en_only.insert("only_en".into(), "e".into());
    let i18n = I18n::from_maps(Language::En, HashMap::from([(Language::En, en_only)]));
    assert_eq!(i18n.t(Language::ZhCN, "only_en"), "e");
}
#[test]
fn t_args_replaces_placeholders() {
    let mut en = HashMap::new(); en.insert("rl".into(), "rate limit exceeded, retry after {}s".into());
    let i18n = I18n::from_maps(Language::En, HashMap::from([(Language::En, en)]));
    assert_eq!(i18n.t_args(Language::En, "rl", &["42".into()]), "rate limit exceeded, retry after 42s");
}
#[test]
fn language_from_str() {
    assert_eq!(Language::from_str_opt("zh"), Language::ZhCN);
    assert_eq!(Language::from_str_opt("zh-CN"), Language::ZhCN);
    assert_eq!(Language::from_str_opt("en"), Language::En);
    assert_eq!(Language::from_str_opt("fr"), Language::En);
}
```
Run: `cargo test -p pichost-core i18n::` — Expected: FAIL(模块不存在)。

**impl_code**(最小实现):
```rust
use std::collections::HashMap;
use tracing::warn;

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub enum Language { En, ZhCN }
impl Language {
    pub fn from_str_opt(s: &str) -> Language {
        match s.to_ascii_lowercase().as_str() {
            "zh" | "zh-cn" => Language::ZhCN,
            "en" => Language::En,
            other => { warn!("unsupported language {other:?}, falling back to en"); Language::En }
        }
    }
    pub fn as_str(self) -> &'static str {
        match self { Language::En => "en", Language::ZhCN => "zh-CN" }
    }
}
#[derive(Debug, Clone)]
pub struct I18n {
    language: Language,
    messages: HashMap<Language, HashMap<String, String>>,
}
impl I18n {
    pub fn from_maps(language: Language, messages: HashMap<Language, HashMap<String, String>>) -> Self {
        Self { language, messages }
    }
    pub fn language(&self) -> Language { self.language }
    fn lookup(&self, locale: Language, key: &str) -> Option<String> {
        self.messages.get(&locale)?.get(key).cloned()
    }
    pub fn t(&self, locale: Language, key: &str) -> String {
        self.lookup(locale, key)
            .or_else(|| self.lookup(Language::En, key))
            .unwrap_or_else(|| key.to_string())
    }
    pub fn t_args(&self, locale: Language, key: &str, args: &[String]) -> String {
        let mut msg = self.t(locale, key);
        for a in args { msg = msg.replacen("{}", a, 1); }
        msg
    }
}
```

**ac:**
- given: 构造含 en/zh 消息表的 I18n 实例
- when: 调用 `t(ZhCN, key)` / `t_args`
- then: 返回对应语言消息;缺失时按 zh→en→key 回退;`{}` 被 args 顺序替换

**regression:**
- `cargo test -p pichost-core test_defaults_app_config -- --exact`
- `cargo test --workspace`

**verify:**
- `cargo test -p pichost-core i18n::`
- `cargo clippy --workspace -- -D warnings`

---

### Task T2: Add embedded locale catalogs and I18n::load

**Files:**
- Create: `pichost-core/src/i18n/locales/en/messages.toml`
- Create: `pichost-core/src/i18n/locales/zh-CN/messages.toml`
- Modify: `pichost-core/src/i18n.rs`(加 `load()` + include_str!)

**Interfaces:**
- Produces: `I18n::load(language: Language, locales_dir: Option<PathBuf>) -> I18n` — 内置 `include_str!` 加载 en/zh-CN;`locales_dir` 为 `Some(dir)` 时,`{dir}/{lang}/messages.toml` 存在则**合并覆盖**(同 key 覆盖、新 key 追加),缺失仅告警回退内置。
- 初始目录键(en 值 = 现有代码原文,zh 为翻译;后续任务持续追加):
```toml
# en/messages.toml
"validation.invalid_credentials" = "invalid username or password"
"validation.password_min_length" = "password must be at least 6 characters"
"common.internal_error" = "internal server error"
# zh-CN/messages.toml
"validation.invalid_credentials" = "用户名或密码错误"
"validation.password_min_length" = "密码至少需要 6 个字符"
"common.internal_error" = "服务器内部错误"
```

**test_code**(先写,验证失败):
```rust
#[test]
fn load_embedded_catalogs() {
    let i18n = I18n::load(Language::En, None);
    assert_eq!(i18n.t(Language::En, "validation.invalid_credentials"), "invalid username or password");
    assert_eq!(i18n.t(Language::ZhCN, "validation.invalid_credentials"), "用户名或密码错误");
}
#[test]
fn load_with_locales_dir_overrides() {
    let dir = std::env::temp_dir().join("pichost-i18n-test");
    let zh_dir = dir.join("zh-CN");
    std::fs::create_dir_all(&zh_dir).unwrap();
    std::fs::write(zh_dir.join("messages.toml"),
        "\"validation.invalid_credentials\" = \"自定义中文\"").unwrap();
    let i18n = I18n::load(Language::ZhCN, Some(dir.clone()));
    assert_eq!(i18n.t(Language::ZhCN, "validation.invalid_credentials"), "自定义中文");
    assert_eq!(i18n.t(Language::En, "validation.invalid_credentials"), "invalid username or password");
    std::fs::remove_dir_all(&dir).ok();
}
```
Run: `cargo test -p pichost-core i18n::` — Expected: FAIL(`load` 不存在)。

**impl_code**(最小实现):
```rust
use std::path::PathBuf;
const EMBEDDED_EN: &str = include_str!("i18n/locales/en/messages.toml");
const EMBEDDED_ZH: &str = include_str!("i18n/locales/zh-CN/messages.toml");
impl I18n {
    pub fn load(language: Language, locales_dir: Option<PathBuf>) -> I18n {
        let mut messages = HashMap::new();
        messages.insert(Language::En, parse_toml(EMBEDDED_EN));
        messages.insert(Language::ZhCN, parse_toml(EMBEDDED_ZH));
        if let Some(dir) = locales_dir {
            for lang in [Language::En, Language::ZhCN] {
                let path = dir.join(lang.as_str()).join("messages.toml");
                if let Ok(content) = std::fs::read_to_string(&path) {
                    messages.entry(lang).or_default().extend(parse_toml(&content));
                } else {
                    warn!("locale file missing: {:?}", path);
                }
            }
        }
        I18n::from_maps(language, messages)
    }
}
fn parse_toml(content: &str) -> HashMap<String, String> {
    toml::from_str(content).unwrap_or_else(|e| { warn!("invalid messages.toml: {e}"); HashMap::new() })
}
```

**ac:**
- given: 无外部目录调用 `I18n::load(En, None)`
- when: 查询 `validation.invalid_credentials`
- then: en 返回 "invalid username or password",zh 返回 "用户名或密码错误"
- given: `locales_dir` 下存在 `zh-CN/messages.toml` 覆盖同 key
- when: `I18n::load(ZhCN, Some(dir))`
- then: zh 返回外部文件值,en 仍内置;文件缺失仅告警不 panic

**regression:**
- `cargo test -p pichost-core i18n::t_`
- `cargo test --workspace`

**verify:**
- `cargo test -p pichost-core i18n::`
- `cargo clippy --workspace -- -D warnings`

---

### Task T3: Add global singleton, reload and lazy mtime hot-check

**Files:**
- Modify: `pichost-core/src/i18n.rs`

**Interfaces:**
- Produces:
```rust
impl I18n {
    pub fn init_global(language: Language, locales_dir: Option<PathBuf>);
    pub fn reload_global(language: Language, locales_dir: Option<PathBuf>);
    pub fn global() -> Arc<I18n>;            // 未初始化时回退默认 en 内置
    pub fn maybe_reload(&mut self) -> bool;  // 惰性 mtime 检查(节流 check_interval,默认 5s)
}
```
- 全局 `static GLOBAL: RwLock<Option<Arc<I18n>>>`;`I18n` 增加字段 `locales_dir: Option<PathBuf>`, `last_check: Instant`, `pub check_interval: Duration`(默认 5s,测试可置 ZERO)。

**test_code**(先写,验证失败):
```rust
#[test]
fn global_singleton_and_reload() {
    I18n::init_global(Language::En, None);
    assert_eq!(I18n::global().t(Language::En, "validation.invalid_credentials"),
        "invalid username or password");
    I18n::reload_global(Language::ZhCN, None);
    assert_eq!(I18n::global().language(), Language::ZhCN);
}
#[test]
fn maybe_reload_detects_external_change() {
    let dir = std::env::temp_dir().join("pichost-i18n-hot");
    let zh_dir = dir.join("zh-CN");
    std::fs::create_dir_all(&zh_dir).unwrap();
    std::fs::write(zh_dir.join("messages.toml"),
        "\"validation.invalid_credentials\" = \"旧值\"").unwrap();
    let mut i18n = I18n::load(Language::ZhCN, Some(dir.clone()));
    assert_eq!(i18n.t(Language::ZhCN, "validation.invalid_credentials"), "旧值");
    i18n.check_interval = std::time::Duration::ZERO;
    std::fs::write(zh_dir.join("messages.toml"),
        "\"validation.invalid_credentials\" = \"新值\"").unwrap();
    assert!(i18n.maybe_reload());
    assert_eq!(i18n.t(Language::ZhCN, "validation.invalid_credentials"), "新值");
    assert!(!i18n.maybe_reload());
    std::fs::remove_dir_all(&dir).ok();
}
```
Run: `cargo test -p pichost-core i18n::` — Expected: FAIL(API 不存在)。

**impl_code**(最小实现):
```rust
use std::sync::{Arc, RwLock};
use std::time::{Duration, Instant};
static GLOBAL: RwLock<Option<Arc<I18n>>> = RwLock::new(None);
// I18n 结构体新增字段:
//   locales_dir: Option<PathBuf>, last_check: Instant, pub check_interval: Duration
// from_maps/load 构造时初始化: last_check: Instant::now(), check_interval: Duration::from_secs(5)
impl I18n {
    pub fn init_global(language: Language, locales_dir: Option<PathBuf>) {
        *GLOBAL.write().unwrap() = Some(Arc::new(I18n::load(language, locales_dir)));
    }
    pub fn reload_global(language: Language, locales_dir: Option<PathBuf>) {
        Self::init_global(language, locales_dir);
    }
    pub fn global() -> Arc<I18n> {
        GLOBAL.read().unwrap().clone()
            .unwrap_or_else(|| Arc::new(I18n::load(Language::En, None)))
    }
    pub fn maybe_reload(&mut self) -> bool {
        let Some(dir) = &self.locales_dir else { return false; };
        if self.last_check.elapsed() < self.check_interval { return false; }
        self.last_check = Instant::now();
        let fresh = I18n::load(self.language, Some(dir.clone()));
        let changed = fresh.messages != self.messages;
        if changed { *self = fresh; }
        changed
    }
}
```

**ac:**
- given: `init_global(En, None)` 后
- when: `I18n::global()`
- then: 返回可用 Arc,查询内置键返回 en 文案;`reload_global(ZhCN, ...)` 后 `language()` 为 ZhCN
- given: 外部消息文件被修改且节流窗口已过
- when: `maybe_reload()`
- then: 返回 true 且后续 t() 返回新值;文件未变返回 false

**regression:**
- `cargo test -p pichost-core i18n::t_`
- `cargo test --workspace`

**verify:**
- `cargo test -p pichost-core i18n::`
- `cargo clippy --workspace -- -D warnings`

---

### Task T4: Add Locale extractor and error_json helpers

**Files:**
- Create: `pichost-api/src/i18n_ext.rs`(含 `#[cfg(test)] mod tests`)
- Modify: `pichost-api/src/lib.rs`(声明 `pub mod i18n_ext;`)

**Interfaces:**
- Produces:
```rust
// pichost-api/src/i18n_ext.rs
pub fn locale_from_header(value: Option<&axum::http::HeaderValue>, fallback: Language) -> Language;
    // 解析 Accept-Language: 逗号分隔 q 权重列表,取首个受支持语言;无/不支持 → fallback
pub struct Locale(pub Language);   // axum::extract::FromRequestParts<AppState>
pub fn error_json(locale: Language, status: StatusCode, key: &str) -> (StatusCode, Json<Value>);
pub fn error_json_args(locale: Language, status: StatusCode, key: &str, args: &[String]) -> (StatusCode, Json<Value>);
pub fn error_json_extra(locale: Language, status: StatusCode, key: &str, extra: Value) -> (StatusCode, Json<Value>);
    // 产出 {"error": I18n::global().t*(locale, key, args), "code": key} + extra 合并
```

**test_code**(先写,验证失败):
```rust
#[test]
fn locale_from_header_resolution() {
    use axum::http::HeaderValue;
    assert_eq!(locale_from_header(Some(&HeaderValue::from_static("zh-CN,zh;q=0.9")), Language::En),
        Language::ZhCN);
    assert_eq!(locale_from_header(Some(&HeaderValue::from_static("fr-FR,fr;q=0.9")), Language::En),
        Language::En);
    assert_eq!(locale_from_header(None, Language::En), Language::En);
}
#[test]
fn error_json_envelope_shape() {
    I18n::init_global(Language::En, None);
    let (status, body) = error_json(Language::ZhCN, StatusCode::UNAUTHORIZED,
        "validation.invalid_credentials");
    assert_eq!(status, StatusCode::UNAUTHORIZED);
    assert_eq!(body.0["error"], "用户名或密码错误");
    assert_eq!(body.0["code"], "validation.invalid_credentials");
}
```
Run: `cargo test -p pichost-api i18n_ext::` — Expected: FAIL(模块不存在)。

**impl_code**(最小实现):
```rust
use axum::extract::FromRequestParts;
use axum::http::header::ACCEPT_LANGUAGE;
use axum::http::{HeaderMap, HeaderValue, StatusCode};
use axum::Json;
use pichost_core::config::AppConfig;
use pichost_core::i18n::{I18n, Language};

pub fn locale_from_header(value: Option<&HeaderValue>, fallback: Language) -> Language {
    let Some(v) = value else { return fallback; };
    v.to_str().ok()
        .and_then(|s| s.split(',').map(|p| p.trim().split(';').next().unwrap_or(""))
            .find_map(|tag| match tag.trim().to_ascii_lowercase().as_str() {
                "zh" | "zh-cn" => Some(Language::ZhCN),
                "en" | "en-us" => Some(Language::En),
                _ => None,
            }))
        .unwrap_or(fallback)
}

#[derive(Debug, Clone, Copy)]
pub struct Locale(pub Language);
impl Locale {
    pub fn from_parts(headers: &HeaderMap, config: &AppConfig) -> Self {
        let fallback = Language::from_str_opt(&config.i18n.language);
        Self(locale_from_header(headers.get(ACCEPT_LANGUAGE), fallback))
    }
}
impl FromRequestParts<crate::app::AppState> for Locale {
    type Rejection = std::convert::Infallible;
    fn from_request_parts(
        parts: &mut axum::http::request::Parts,
        state: &crate::app::AppState,
    ) -> Result<Self, Self::Rejection> {
        Ok(Self::from_parts(&parts.headers, &state.config))
    }
}

fn envelope(locale: Language, key: &str, args: &[String], extra: Value) -> Value {
    let msg = if args.is_empty() { I18n::global().t(locale, key) }
              else { I18n::global().t_args(locale, key, args) };
    let mut v = serde_json::json!({ "error": msg, "code": key });
    if let Some(o) = v.as_object_mut() {
        if let Some(x) = extra.as_object() { o.extend(x.clone()); }
    }
    v
}
pub fn error_json(locale: Language, status: StatusCode, key: &str) -> (StatusCode, Json<Value>) {
    (status, Json(envelope(locale, key, &[], serde_json::json!({}))))
}
pub fn error_json_args(locale: Language, status: StatusCode, key: &str, args: &[String]) -> (StatusCode, Json<Value>) {
    (status, Json(envelope(locale, key, args, serde_json::json!({}))))
}
pub fn error_json_extra(locale: Language, status: StatusCode, key: &str, extra: Value) -> (StatusCode, Json<Value>) {
    (status, Json(envelope(locale, key, &[], extra)))
}
```

**ac:**
- given: Accept-Language 为 "zh-CN,zh;q=0.9"
- when: `locale_from_header`
- then: 返回 ZhCN;不支持语言或无头返回 fallback
- given: `error_json(ZhCN, 401, "validation.invalid_credentials")`
- then: body 为 `{"error":"用户名或密码错误","code":"validation.invalid_credentials"}`,status 401

**regression:**
- `cargo test -p pichost-core i18n::`
- `cargo test --workspace`

**verify:**
- `cargo test -p pichost-api i18n_ext::`
- `cargo clippy --workspace -- -D warnings`

---

### Task T5: Add code to AppError envelope and localize Json rejections

**Files:**
- Modify: `pichost-core/src/error.rs`(含测试)
- Modify: `pichost-api/src/i18n_ext.rs`(追加 `JsonBody<T>`)

**Interfaces:**
- Consumes: `i18n_ext::{error_json, locale_from_header}`;`I18n::global()`
- Produces: `AppError::code(&self) -> &'static str`: `Authentication→"auth_failed"`, `Authorization→"auth_insufficient_permissions"`, `NotFound→"not_found"`, `Validation→"validation_error"`, `Upload→"upload_failed"`, `RateLimited→"rate_limited"`, `Storage(PayloadTooLarge)→"storage_payload_too_large"`, 其余 → `"internal_error"`;`IntoResponse` 输出 `{"error": msg, "code": code}`(消息保持现有 en 字面量)。
- Produces: `JsonBody<T>(pub T)` — 代理 `axum::Json<T>`,rejection 映射为 `error_json(locale, status, "validation_error")`,locale 从请求头解析(无 AppState 上下文,fallback = `I18n::global().language()`)。后续转换任务把路由 `Json<X>` 换为 `JsonBody<X>`。

**test_code**(先写,验证失败):
```rust
// error.rs 追加
#[test]
fn app_error_codes() {
    assert_eq!(AppError::Authentication("a".into()).code(), "auth_failed");
    assert_eq!(AppError::NotFound("a".into()).code(), "not_found");
    assert_eq!(AppError::Validation("a".into()).code(), "validation_error");
    assert_eq!(AppError::RateLimited.code(), "rate_limited");
    assert_eq!(AppError::Storage(StorageError::PayloadTooLarge("a".into())).code(),
        "storage_payload_too_large");
    assert_eq!(AppError::Internal("a".into()).code(), "internal_error");
}
// i18n_ext.rs 追加
#[tokio::test]
async fn json_body_rejection_localized() {
    use axum::extract::FromRequestParts;
    use axum::http::Request;
    I18n::init_global(Language::ZhCN, None);
    let req = Request::builder()
        .header("content-type", "application/json")
        .header("accept-language", "zh-CN")
        .body(axum::body::Body::from("{\"bad json"))
        .unwrap();
    let (mut parts, _body) = req.into_parts();
    let resp = JsonBody::<serde_json::Value>::from_request_parts(&mut parts, &())
        .await.unwrap_err().into_response();
    let bytes = axum::body::to_bytes(resp.into_body(), usize::MAX).await.unwrap();
    let v: serde_json::Value = serde_json::from_slice(&bytes).unwrap();
    assert_eq!(v["code"], "validation_error");
    assert!(v["error"].is_string());
}
```
Run: `cargo test -p pichost-core app_error_codes -- --exact` — FAIL(`code()` 不存在);`cargo test -p pichost-api json_body_rejection_localized` — FAIL(`JsonBody` 不存在)。

**impl_code**(最小实现):
```rust
// error.rs
impl AppError {
    pub fn code(&self) -> &'static str {
        match self {
            Self::Authentication(_) => "auth_failed",
            Self::Authorization(_) => "auth_insufficient_permissions",
            Self::NotFound(_) => "not_found",
            Self::Validation(_) => "validation_error",
            Self::Upload(_) => "upload_failed",
            Self::RateLimited => "rate_limited",
            Self::Storage(e) => match e {
                StorageError::PayloadTooLarge(_) => "storage_payload_too_large",
                _ => "internal_error",
            },
            Self::Internal(_) => "internal_error",
        }
    }
}
// IntoResponse 尾部改为:
//   (status, Json(serde_json::json!({ "error": msg, "code": self.code() }))).into_response()

// i18n_ext.rs 追加
pub struct JsonBody<T>(pub T);
impl<T, S> FromRequestParts<S> for JsonBody<T>
where
    T: serde::de::DeserializeOwned,
    S: Send + Sync,
{
    type Rejection = axum::response::Response;
    async fn from_request_parts(
        parts: &mut axum::http::request::Parts,
        state: &S,
    ) -> Result<Self, Self::Rejection> {
        let fallback = I18n::global().language();
        match axum::Json::<T>::from_request_parts(parts, state).await {
            Ok(axum::Json(v)) => Ok(JsonBody(v)),
            Err(rejection) => {
                let locale = locale_from_header(parts.headers.get(ACCEPT_LANGUAGE), fallback);
                Err(error_json(locale, rejection.status(), "validation_error").into_response())
            }
        }
    }
}
```
注意: 既有 error.rs 测试 `into_response_status_map` 仅断言 status,不受 code 字段影响。

**ac:**
- given: `AppError::Validation("bad")`
- when: `into_response()`
- then: body 含 `"error":"bad"` 与 `"code":"validation_error"`
- given: 畸形 JSON 请求 + Accept-Language: zh-CN
- when: `JsonBody` 提取
- then: 422 + `{"error": <中文>, "code":"validation_error"}`

**regression:**
- `cargo test -p pichost-core into_response_status_map -- --exact`
- `cargo test -p pichost-api i18n_ext::`
- `cargo test --workspace`

**verify:**
- `cargo test -p pichost-core app_error_codes -- --exact`
- `cargo test -p pichost-api json_body_rejection_localized`
- `cargo clippy --workspace -- -D warnings`

---

### Task T6: Convert auth.rs errors to localized keys

**Files:**
- Modify: `pichost-api/src/routes/auth.rs`
- Modify: `pichost-api/tests/auth_test.rs`

**Interfaces:**
- Consumes: `i18n_ext::{error_json, error_json_args, Locale, JsonBody}`
- Produces: auth.rs 全部错误经 `error_json(Locale.0, status, key)` / `error_json_args` 返回;handler 签名加 `locale: Locale`;请求体 `Json<T>` → `JsonBody<T>`;成功消息 `{"message": ...}` 保持字面量不变。
- 新增目录键(en 值 = 现有代码原文,zh 为翻译;追加到 `pichost-core/src/i18n/locales/{en,zh-CN}/messages.toml`):
```toml
# en
"auth.invalid_credentials" = "invalid username or password"
"auth.invalid_token" = "invalid or expired token"
"auth.invalid_refresh_token" = "invalid or expired refresh token"
"auth.revoked_token" = "refresh token has been revoked"
"auth.invalid_subject" = "invalid token subject"
"auth.token_generation_failed" = "token generation failed"
"auth.missing_header" = "missing authorization header"
"auth.invalid_header_format" = "invalid authorization format"
"auth.logout_access_only" = "only access tokens can be logged out via this endpoint"
"invite.required" = "invite code is required"
"invite.invalid" = "invalid invite code"
"invite.expired" = "invite code has expired"
"invite.used" = "invite code has already been used"
"auth.username_exists" = "username or email already exists"
"validation.password_min_length" = "password must be at least 6 characters"
"common.internal_error" = "internal server error"
# zh-CN(翻译)
"auth.invalid_credentials" = "用户名或密码错误"
"auth.invalid_token" = "令牌无效或已过期"
"auth.invalid_refresh_token" = "刷新令牌无效或已过期"
"auth.revoked_token" = "刷新令牌已被吊销"
"auth.invalid_subject" = "令牌主体无效"
"auth.token_generation_failed" = "令牌生成失败"
"auth.missing_header" = "缺少授权头"
"auth.invalid_header_format" = "授权格式无效"
"auth.logout_access_only" = "仅访问令牌可通过此端点登出"
"invite.required" = "需要邀请码"
"invite.invalid" = "邀请码无效"
"invite.expired" = "邀请码已过期"
"invite.used" = "邀请码已被使用"
"auth.username_exists" = "用户名或邮箱已存在"
"validation.password_min_length" = "密码至少需要 6 个字符"
"common.internal_error" = "服务器内部错误"
```

**test_code**(先写,验证失败;追加到 `tests/auth_test.rs`):
```rust
#[tokio::test]
#[ignore]
async fn test_login_failure_returns_code_and_localized_message() {
    let app = test_app().await;
    let resp = send_json(&app, "/api/v1/auth/login", &json!({"username":"nobody","password":"x"})).await;
    assert_eq!(resp.status(), StatusCode::UNAUTHORIZED);
    let body: serde_json::Value = resp.json().await;
    assert_eq!(body["code"], "auth.invalid_credentials");
}
#[tokio::test]
#[ignore]
async fn test_login_failure_zh_negotiation() {
    let app = test_app().await;
    let resp = app.clone().oneshot(Request::builder()
        .method("POST").uri("/api/v1/auth/login")
        .header("content-type", "application/json")
        .header("accept-language", "zh-CN")
        .body(Body::from(r#"{"username":"nobody","password":"x"}"#)).unwrap())
        .await.unwrap();
    let bytes = axum::body::to_bytes(resp.into_body(), usize::MAX).await.unwrap();
    let body: serde_json::Value = serde_json::from_slice(&bytes).unwrap();
    assert_eq!(body["error"], "用户名或密码错误");
    assert_eq!(body["code"], "auth.invalid_credentials");
}
```
Run: `cargo test -p pichost-api --test auth_test test_login_failure_returns_code_and_localized_message -- --include-ignored --exact` — Expected: FAIL(body 无 `code` 字段)。

**impl_code**(最小实现):
删除本地 `error_response(status, message)` helper;每处 `error_response(STATUS, "literal")` 改为 `error_json(locale.0, STATUS, "domain.key")`(键值对照上表,字面量必须与现有代码完全一致);对应 handler 增加 `locale: Locale` 提取参数;`Json<X>` 请求体参数换为 `JsonBody<X>`;同步追加上表目录键。

**ac:**
- given: 登录失败请求
- when: 检查响应
- then: status 401 且 body 含 `code:"auth.invalid_credentials"` 与 en 消息;带 `accept-language: zh-CN` 时 `error` 为中文
- given: 无邀请码注册
- when: 检查响应
- then: body 含 `code:"invite.required"`

**regression:**
- `cargo test -p pichost-api --test auth_test -- --include-ignored`
- `cargo test --workspace`

**verify:**
- `cargo test -p pichost-api --test auth_test test_login_failure_zh_negotiation -- --include-ignored --exact`
- `cargo clippy --workspace -- -D warnings`

---

### Task T7: Convert images.rs errors to localized keys

**Files:**
- Modify: `pichost-api/src/routes/images.rs`
- Modify: `pichost-api/tests/images_test.rs`

**Interfaces:**
- Consumes: `i18n_ext::{error_json, error_json_args, error_json_extra, Locale, JsonBody}`
- 新增目录键(en 原文;zh 翻译对应):
```toml
"image.not_found" = "image not found"
"image.thumbnail_not_ready" = "thumbnail not yet generated"
"image.thumbnail_not_found" = "thumbnail not found"
"image.webp_not_ready" = "WebP not yet generated"
"image.webp_not_found" = "WebP not found"
"image.batch_empty" = "no image ids provided"
"image.batch_limit" = "batch limit is 100 images"
"image.rename_empty" = "original_name cannot be empty"
"image.rename_too_long" = "original_name too long (max 255)"
"image.rename_invalid" = "original_name contains invalid characters"
"category.not_found" = "Category not found"
"common.internal_error" = "internal server error"
```

**test_code**(先写,验证失败;追加到 `tests/images_test.rs`):
```rust
#[tokio::test]
#[ignore]
async fn test_get_missing_image_returns_code() {
    let app = test_app().await;
    let user = create_user(&app).await;
    let resp = send_json_auth(&app, "GET", "/api/v1/images/00000000-0000-0000-0000-000000000000", &user).await;
    assert_eq!(resp.status(), StatusCode::NOT_FOUND);
    let body: serde_json::Value = resp.json().await;
    assert_eq!(body["code"], "image.not_found");
}
```
Run: `cargo test -p pichost-api --test images_test test_get_missing_image_returns_code -- --include-ignored --exact` — FAIL。

**impl_code**(最小实现):
同 T6 模式:全部 `json!({"error": "..."})` 与 `(status, Json(json!({"error": msg})))` 改经 `error_json/error_json_args/error_json_extra(locale.0, ...)`;handler 加 `locale: Locale`;`Json<X>` → `JsonBody<X>`;批量删除/移动成功消息保持 `{"message": ...}`;目录追加上表键。

**ac:**
- given: 访问不存在的图片
- then: 404 + `code:"image.not_found"`
- given: 批量删除空数组
- then: 400 + `code:"image.batch_empty"`

**regression:**
- `cargo test -p pichost-api --test images_test -- --include-ignored`
- `cargo test --workspace`

**verify:**
- `cargo test -p pichost-api --test images_test test_get_missing_image_returns_code -- --include-ignored --exact`
- `cargo clippy --workspace -- -D warnings`

---

### Task T8: Convert upload services to localized keys

**Files:**
- Modify: `pichost-api/src/services/upload.rs`
- Modify: `pichost-api/src/services/upload_url.rs`
- Modify: `pichost-api/tests/images_test.rs`

**Interfaces:**
- Consumes: `i18n_ext::{error_json, error_json_args, error_json_extra, Locale}`
- 新增目录键(en 原文;含 `{}` 占位模板,zh 对应翻译):
```toml
"upload.file_missing" = "no file field found in upload"
"upload.file_too_large" = "file exceeds maximum allowed size"
"upload.quota_exceeded" = "storage quota exceeded"
"upload.not_an_image" = "file is not a valid image"
"upload.save_failed" = "failed to save image metadata"
"upload.backend_resolve_failed" = "failed to resolve storage backend"
"upload.configs_none_match" = "storage_config_ids: no matching configs found"
"upload.configs_too_many" = "at most 2 storage_config_ids are allowed"
"upload.configs_need_local" = "at least one storage config must use 'local' provider"
"upload.encryption_key_invalid" = "server misconfiguration: invalid encryption key"
"url.invalid" = "invalid URL: {}"
"url.unsupported_scheme" = "unsupported URL scheme: {} (only http/https allowed)"
"url.resolve_failed" = "failed to resolve host: {}"
"url.no_addresses" = "URL host resolved to zero addresses"
"url.ssrf_blocked" = "URL resolves to a private/internal address — SSRF blocked"
"url.client_failed" = "failed to build HTTP client: {}"
"url.timed_out" = "download timed out (30s)"
"url.connect_failed" = "failed to connect: {}"
"url.too_many_redirects" = "too many redirects (max 5)"
"url.download_failed" = "download failed: {}"
"url.server_error" = "remote server returned {} {}"
"url.too_large" = "response exceeds maximum size (50 MB), got {} bytes"
"url.body_failed" = "failed to read body: {}"
"url.not_an_image" = "downloaded content is not a valid image"
"url.no_host" = "URL has no host"
"common.internal_error" = "internal server error"
"common.internal_error_detail" = "internal error: {}"
```
- 注意: upload.rs 配额 413 body 保留附加字段 → `error_json_extra(locale.0, StatusCode::PAYLOAD_TOO_LARGE, "upload.quota_exceeded", json!({"quota_bytes": q, "used_bytes": u, "file_bytes": f}))`;upload_url.rs 动态消息(含 `{}`)用 `error_json_args`。

**test_code**(先写,验证失败;追加到 `tests/images_test.rs`):
```rust
#[tokio::test]
#[ignore]
async fn test_quota_exceeded_keeps_extra_fields_and_code() {
    let app = test_app().await;
    // 若 harness 无 create_user_with_quota: 先 create_user,再以 admin PATCH /api/v1/admin/users/{id}
    // 设置 storage_quota=1(参照既有 quota 测试模式),最后上传 tiny_png
    let user = create_user_with_quota(&app, 1).await;
    let resp = upload_file(&app, &user, "tiny.png", tiny_png()).await;
    assert_eq!(resp.status(), StatusCode::PAYLOAD_TOO_LARGE);
    let body: serde_json::Value = resp.json().await;
    assert_eq!(body["code"], "upload.quota_exceeded");
    assert!(body.get("quota_bytes").is_some());
}
```
Run: `cargo test -p pichost-api --test images_test test_quota_exceeded_keeps_extra_fields_and_code -- --include-ignored --exact` — FAIL(body 无 code)。

**impl_code**(最小实现):
同 T6 模式;upload.rs / upload_url.rs 的 `ApiError`/`err()` 本地 helper 改经 `error_json*` 构造;`err(msg)` 签名改为 `err(locale, key, args)`;目录追加上表键(含 `{}` 占位模板)。

**ac:**
- given: quota 超限上传
- then: 413 + `code:"upload.quota_exceeded"` 且保留 quota_bytes/used_bytes/file_bytes
- given: 上传非图片文件
- then: 400 + `code:"upload.not_an_image"`

**regression:**
- `cargo test -p pichost-api --test images_test -- --include-ignored`
- `cargo test --workspace`

**verify:**
- `cargo test -p pichost-api --test images_test test_quota_exceeded_keeps_extra_fields_and_code -- --include-ignored --exact`
- `cargo clippy --workspace -- -D warnings`

---

### Task T9: Convert users.rs and categories.rs errors

**Files:**
- Modify: `pichost-api/src/routes/users.rs`
- Modify: `pichost-api/src/routes/categories.rs`
- Modify: `pichost-api/tests/users_test.rs`

**Interfaces:**
- 新增目录键(en 原文;zh 对应翻译):
```toml
"user.not_found" = "user not found"
"user.username_taken" = "username already taken"
"user.email_taken" = "email already taken"
"user.username_email_taken" = "username or email already exists"
"user.unknown_backend" = "unknown backend: {}"
"user.current_password_incorrect" = "current password incorrect"
"user.password_too_weak" = "new password must be at least 8 characters"
"user.watermark_invalid" = "invalid watermark config"
"category.not_found" = "Category not found"
"category.parent_not_found" = "Parent category not found"
"category.name_exists" = "Category name already exists"
"category.depth_exceeded" = "Maximum category depth is {}"
"category.invalid_name" = "Name must be 1-128 characters"
"common.internal_error" = "internal server error"
```
- 顺带修复: categories.rs 中裸 `e.to_string()` 泄漏 sqlx 错误的位置统一为 `error_json_args(locale.0, StatusCode::INTERNAL_SERVER_ERROR, "common.internal_error_detail", &[e.to_string()])`。

**test_code**(先写,验证失败;追加到 `tests/users_test.rs`):
```rust
#[tokio::test]
#[ignore]
async fn test_username_taken_returns_code() {
    let app = test_app().await;
    let user = create_user(&app).await;
    let resp = send_json_auth(&app, "PATCH", "/api/v1/users/me", &user,
        &json!({"username": user.username})).await;
    assert_eq!(resp.status(), StatusCode::CONFLICT);
    let body: serde_json::Value = resp.json().await;
    assert_eq!(body["code"], "user.username_taken");
}
```
Run: `cargo test -p pichost-api --test users_test test_username_taken_returns_code -- --include-ignored --exact` — FAIL。

**impl_code**(最小实现):
同 T6 模式;两文件全部错误改经 `error_json*`;`Json<X>` → `JsonBody<X>`;目录追加上表键。

**ac:**
- given: 修改用户名为已占用值
- then: 409 + `code:"user.username_taken"`
- given: 创建深度超限分类
- then: 400 + `code:"category.depth_exceeded"`

**regression:**
- `cargo test -p pichost-api --test users_test -- --include-ignored`
- `cargo test -p pichost-api --test categories_test -- --include-ignored`
- `cargo test --workspace`

**verify:**
- `cargo test -p pichost-api --test users_test test_username_taken_returns_code -- --include-ignored --exact`
- `cargo clippy --workspace -- -D warnings`

---

### Task T10: Convert oauth and middleware errors

**Files:**
- Modify: `pichost-api/src/routes/oauth.rs`
- Modify: `pichost-api/src/middleware/auth.rs`
- Modify: `pichost-api/src/middleware/rate_limit.rs`

**Interfaces:**
- Consumes: `i18n_ext::{error_json, error_json_args, Locale, locale_from_header}`;middleware 无法用 extractor → 直接用 `locale_from_header(req.headers().get(ACCEPT_LANGUAGE), I18n::global().language())`。
- 新增目录键(en 原文;zh 对应翻译):
```toml
"auth.oauth_not_configured" = "{} OAuth client_id not configured"
"auth.oauth_secret_not_configured" = "{} OAuth client_secret not configured"
"auth.unknown_provider" = "unknown provider"
"auth.invalid_oauth_code" = "invalid authorization code"
"auth.oauth_userinfo_failed" = "failed to fetch user info"
"auth.oauth_no_link" = "no account linked. Register first, then link OAuth in Settings."
"auth.missing_header" = "missing authorization header"
"auth.invalid_header_format" = "invalid authorization format"
"auth.invalid_token" = "invalid or expired token"
"auth.revoked_token" = "token has been revoked"
"auth.invalid_subject" = "invalid token subject"
"auth.authentication_required" = "authentication required"
"auth.admin_required" = "admin access required"
"rate_limited" = "rate limit exceeded, retry after {}s"
"common.internal_error" = "internal server error"
```

**test_code**(先写,验证失败;追加到 `tests/auth_test.rs`):
```rust
#[tokio::test]
#[ignore]
async fn test_admin_endpoint_forbidden_code() {
    let app = test_app().await;
    let user = create_user(&app).await; // 非 admin
    let resp = send_json_auth(&app, "GET", "/api/v1/admin/stats", &user).await;
    assert_eq!(resp.status(), StatusCode::FORBIDDEN);
    let body: serde_json::Value = resp.json().await;
    assert_eq!(body["code"], "auth.admin_required");
}
#[tokio::test]
#[ignore]
async fn test_rate_limit_returns_code() {
    // 若 harness 无 test_app_with_rate_limits: 参照 common/mod.rs 现有 test_app 实现,
    // 构造 AppConfig{ rate_limit: RateLimitConfig{ auth_max: 2, ..Default::default() } } 的应用
    let app = test_app_with_rate_limits(&json!({"auth_max": 2})).await;
    for _ in 0..3 {
        send_json(&app, "/api/v1/auth/login", &json!({"username":"x","password":"y"})).await;
    }
    let resp = send_json(&app, "/api/v1/auth/login", &json!({"username":"x","password":"y"})).await;
    assert_eq!(resp.status(), StatusCode::TOO_MANY_REQUESTS);
    let body: serde_json::Value = resp.json().await;
    assert_eq!(body["code"], "rate_limited");
}
```
Run: `cargo test -p pichost-api --test auth_test test_admin_endpoint_forbidden_code -- --include-ignored --exact` — FAIL(body 无 code)。

**impl_code**(最小实现):
同 T6 模式;oauth.rs handler 加 `locale: Locale`;middleware 两处从请求头解析 locale;`too_many_response(retry_after)` 改为 `error_json_args(locale, StatusCode::TOO_MANY_REQUESTS, "rate_limited", &[retry_after.to_string()])`;目录追加上表键。

**ac:**
- given: 非 admin 访问 admin 端点
- then: 403 + `code:"auth.admin_required"`
- given: 触发限流
- then: 429 + `code:"rate_limited"` 且消息含 retry 秒数

**regression:**
- `cargo test -p pichost-api --test auth_test -- --include-ignored`
- `cargo test --workspace`

**verify:**
- `cargo test -p pichost-api --test auth_test test_rate_limit_returns_code -- --include-ignored --exact`
- `cargo clippy --workspace -- -D warnings`

---

### Task T11: Convert admin.rs and storage_configs.rs errors

**Files:**
- Modify: `pichost-api/src/routes/admin.rs`
- Modify: `pichost-api/src/routes/storage_configs.rs`
- Modify: `pichost-api/tests/admin_test.rs`

**Interfaces:**
- Consumes: `i18n_ext::{error_json, error_json_args, error_json_extra, Locale, JsonBody}`
- storage_configs.rs 从 `Result<_, AppError>` 改为经 helper 显式本地化(12 处);其原中文消息移入 zh 目录,en 提供英文等价:
```toml
# en
"storage_config.limit" = "maximum {} storage configs allowed"
"storage_config.name_exists" = "storage config name already exists"
"storage_config.not_found" = "storage config not found"
"storage_config.unsupported_type" = "unsupported storage type"
"storage_config.repo_unreachable" = "cannot reach repository: {}"
"storage_config.token_invalid" = "token is invalid or lacks repository access"
"storage_config.repo_not_found" = "repository not found, check owner/repo"
"storage_config.repo_verify_failed" = "repository verification failed ({}): {}"
"storage_config.unsupported_provider" = "unsupported storage type, only github and gitcode are supported"
"storage_config.encryption_key_missing" = "encryption key is not configured"
"storage_config.in_use" = "images exist under this config, delete them first"
"storage_payload_too_large" = "payload too large: {}"
"admin.password_too_weak" = "password must be at least 8 characters"
"admin.cannot_demote_self" = "cannot demote yourself"
"admin.cannot_delete_self" = "cannot delete yourself"
"config.write_failed" = "write failed: {}"
"config.materialize_failed" = "materialize config failed: {}"
"config.backup_not_found" = "Backup not found: {}"
"common.internal_error" = "internal server error"
# zh-CN: storage_config.* 直接用原中文文案("最多只能创建{}个存储配置" 等),其余翻译
```
- 测试迁移: `tests/admin_test.rs` 中断言中文消息的用例改为断言 `code`(如 "配置名称已存在" → `body["code"] == "storage_config.name_exists"`)。

**test_code**(先写,验证失败;追加到 `tests/admin_test.rs`):
```rust
#[tokio::test]
#[ignore]
async fn test_duplicate_storage_config_name_returns_code() {
    let app = test_app().await;
    let user = create_user(&app).await;
    // 若 harness 无 create_storage_config: 直接 send_json_auth POST /api/v1/users/me/storage-configs
    // body {"name":"repo","provider":"github","token":"x","repo":"owner/repo"}
    let _ = create_storage_config(&app, &user, "repo", "github").await;
    let resp = create_storage_config(&app, &user, "repo", "github").await;
    assert_eq!(resp.status(), StatusCode::CONFLICT);
    let body: serde_json::Value = resp.json().await;
    assert_eq!(body["code"], "storage_config.name_exists");
}
```
Run: `cargo test -p pichost-api --test admin_test test_duplicate_storage_config_name_returns_code -- --include-ignored --exact` — FAIL。

**impl_code**(最小实现):
admin.rs: `internal_error(msg)` helper 改为经 `error_json_args(locale.0, StatusCode::INTERNAL_SERVER_ERROR, key, args)`;handler 加 `locale: Locale`;`Json<X>` → `JsonBody<X>`。storage_configs.rs: 每个 handler 的 `Err(e) => e.into_response()` 路径替换为显式 `error_json*`(status 映射: Validation→400、NotFound→404、Conflict→409、Internal→500);目录追加上表键。

**ac:**
- given: 重复创建同名存储配置
- then: 409 + `code:"storage_config.name_exists"`(en 消息英文,zh 消息为原中文)
- given: 删除仍有关联图片的配置
- then: 409 + `code:"storage_config.in_use"`

**regression:**
- `cargo test -p pichost-api --test admin_test -- --include-ignored`
- `cargo test --workspace`

**verify:**
- `cargo test -p pichost-api --test admin_test test_duplicate_storage_config_name_returns_code -- --include-ignored --exact`
- `cargo clippy --workspace -- -D warnings`

---

### Task T12: Wire i18n into config service and startup

**Files:**
- Modify: `pichost-api/src/services/config.rs`
- Modify: `pichost-api/src/routes/admin.rs`
- Modify: `pichost-api/src/app.rs`

**Interfaces:**
- Consumes: `I18n::{init_global, reload_global}`
- Produces:
  - `SystemConfig` 读写映射增加 `i18n` 嵌套键(`[i18n] language` / `[i18n] locales_dir`,参照现有 `[server] public_url` 嵌套键实现;`read_config_toml` 输出、`write_config_toml` 写回)。
  - API 启动路径(`main.rs` 或 `configure_app` 前)调用 `I18n::init_global(Language::from_str_opt(&config.i18n.language), config.i18n.locales_dir.clone())`。
  - `update_admin_config` handler 写入成功后调用 `I18n::reload_global(Language::from_str_opt(&new_cfg.i18n.language), new_cfg.i18n.locales_dir.clone())`。

**test_code**(先写,验证失败;追加到 `tests/admin_test.rs`,参照既有 config 端点测试的 `#[serial]` + config.toml 清理模式):
```rust
#[tokio::test]
#[serial]
#[ignore]
async fn test_admin_config_roundtrip_i18n_language() {
    let app = test_app().await;
    let admin = create_admin(&app).await;
    // PUT /api/v1/admin/config 携带 {"i18n": {"language": "zh-CN"}}
    let resp = send_json_auth(&app, "PUT", "/api/v1/admin/config", &admin,
        &json!({"i18n": {"language": "zh-CN"}})).await;
    assert!(resp.status().is_success());
    // GET 返回 language=zh-CN
    let resp = send_json_auth(&app, "GET", "/api/v1/admin/config", &admin).await;
    let body: serde_json::Value = resp.json().await;
    assert_eq!(body["i18n"]["language"], "zh-CN");
    // reload 已触发: 全局语言立即为 ZhCN
    assert_eq!(pichost_core::i18n::I18n::global().language(),
        pichost_core::i18n::Language::ZhCN);
}
```
Run: `cargo test -p pichost-api --test admin_test test_admin_config_roundtrip_i18n_language -- --include-ignored --exact` — FAIL(config 无 i18n 字段)。

**impl_code**(最小实现):
config.rs 服务: `SystemConfig` 结构/读写映射增加 i18n 键(嵌套键实现);app.rs: 启动路径(或 `configure_app`)在 `AppState` 构造后调用 `init_global`;admin.rs `update_admin_config` 成功后调 `reload_global`。

**ac:**
- given: PUT /admin/config 携带 `i18n.language=zh-CN`
- then: GET 返回该值,且 `I18n::global().language()` 立即为 ZhCN(无需重启)
- given: API 启动(无 env)
- then: `I18n::global()` 可用,默认 en

**regression:**
- `cargo test -p pichost-api --test admin_test -- --include-ignored`
- `cargo test --workspace`

**verify:**
- `cargo test -p pichost-api --test admin_test test_admin_config_roundtrip_i18n_language -- --include-ignored --exact`
- `cargo clippy --workspace -- -D warnings`

---

## 前端任务

### Task F1: Add i18next engine and dependencies

**Files:**
- Modify: `web-ui/package.json`
- Create: `web-ui/src/i18n/index.ts`
- Create: `web-ui/src/i18n/i18n.test.ts`

**Interfaces:**
- Produces: `src/i18n/index.ts` 导出全局 i18n 实例(初始化 i18next + LanguageDetector + initReactI18next,resources 从 `./locales/en.json` / `./locales/zh-CN.json` 导入 — F2 创建);`export function getCurrentLocale(): string`(`zh*` → `'zh-CN'`,否则 `'en'`);初始化时立即执行 `applyLang` 同步 `document.documentElement.lang` 并注册 `languageChanged` 监听。

**test_code**(先写,验证失败):
```ts
// src/i18n/i18n.test.ts
import { describe, it, expect, beforeEach } from 'vitest'
import i18n, { getCurrentLocale } from './index'

describe('i18n engine', () => {
  beforeEach(async () => {
    localStorage.clear()
    await i18n.changeLanguage('en')
  })
  it('returns normalized locale after changeLanguage', async () => {
    await i18n.changeLanguage('zh-CN')
    expect(getCurrentLocale()).toBe('zh-CN')
    expect(document.documentElement.lang).toBe('zh-CN')
  })
  it('normalizes zh to zh-CN', async () => {
    await i18n.changeLanguage('zh')
    expect(getCurrentLocale()).toBe('zh-CN')
  })
})
```
Run: `npx vitest run src/i18n/i18n.test.ts` — Expected: FAIL(module 不存在)。

**impl_code**(最小实现):
```bash
cd web-ui && npm install i18next react-i18next i18next-browser-languagedetector
```
```ts
// src/i18n/index.ts
import i18n from 'i18next'
import { initReactI18next } from 'react-i18next'
import LanguageDetector from 'i18next-browser-languagedetector'
import en from './locales/en.json'
import zhCN from './locales/zh-CN.json'

export function getCurrentLocale(): string {
  return i18n.language.toLowerCase().startsWith('zh') ? 'zh-CN' : 'en'
}
export function applyLang(lng: string): void {
  document.documentElement.lang = lng.toLowerCase().startsWith('zh') ? 'zh-CN' : 'en'
}

i18n.use(LanguageDetector)
    .use(initReactI18next)
    .init({
      resources: { en: { translation: en }, 'zh-CN': { translation: zhCN } },
      supportedLngs: ['en', 'zh-CN'],
      fallbackLng: 'en',
      nonExplicitSupportedLngs: true,
      detection: {
        order: ['localStorage', 'navigator'],
        caches: ['localStorage'],
        localStorageKey: 'pichost-locale',
      },
      interpolation: { escapeValue: false },
      ns: ['translation'],
    })

applyLang(i18n.language)
i18n.on('languageChanged', applyLang)

export default i18n
```

**ac:**
- given: `changeLanguage('zh-CN')`
- then: `getCurrentLocale()` 返回 'zh-CN' 且 `document.documentElement.lang` 同步为 'zh-CN'
- given: `changeLanguage('zh')`
- then: `getCurrentLocale()` 规范化为 'zh-CN'

**regression:**
- `npx vitest run src/stores/__tests__/preprocessing.test.ts`(既有测试不受影响)

**verify:**
- `npx vitest run src/i18n/i18n.test.ts`
- `npm run build`(F2 完成后可过;F1 阶段若 en.json 缺失属预期中间态)

---

### Task F2: Create initial locale catalogs with key-consistency test

**Files:**
- Create: `web-ui/src/i18n/locales/en.json`
- Create: `web-ui/src/i18n/locales/zh-CN.json`
- Modify: `web-ui/src/i18n/i18n.test.ts`

**Interfaces:**
- Produces: 初始目录含 `common.*` 与 `nav.*` 键;`i18n.test.ts` 增加**键一致性测试**(en/zh 键集必须完全相等,所有抽取任务持续维护):
```json
// en.json
{
  "common": {
    "cancel": "Cancel",
    "save": "Save",
    "loading": "Loading…",
    "delete": "Delete",
    "internalError": "Something went wrong"
  },
  "nav": {
    "dashboard": "Dashboard",
    "gallery": "Gallery",
    "admin": "Admin",
    "settings": "Settings",
    "logout": "Logout"
  }
}
```
zh-CN.json 键结构与 en 完全一致,值为中文。

**test_code**(先写,验证失败):
```ts
// i18n.test.ts 追加
function flattenKeys(obj: Record<string, unknown>, prefix = ''): string[] {
  return Object.entries(obj).flatMap(([k, v]) =>
    v && typeof v === 'object' ? flattenKeys(v as Record<string, unknown>, `${prefix}${k}.`)
                               : [`${prefix}${k}`])
}
it('en and zh-CN key sets are identical', async () => {
  const en = (await import('./locales/en.json')).default
  const zh = (await import('./locales/zh-CN.json')).default
  const enKeys = flattenKeys(en).sort()
  const zhKeys = flattenKeys(zh).sort()
  expect(zhKeys).toEqual(enKeys)
})
it('common and nav prefixes exist in both locales', async () => {
  const en = (await import('./locales/en.json')).default as Record<string, unknown>
  expect(Object.keys(en)).toEqual(expect.arrayContaining(['common', 'nav']))
  const zh = (await import('./locales/zh-CN.json')).default as Record<string, unknown>
  expect(Object.keys(zh)).toEqual(expect.arrayContaining(['common', 'nav']))
})
```
Run: `npx vitest run src/i18n/i18n.test.ts` — Expected: FAIL(JSON 文件缺失)。

**impl_code**(最小实现):
创建上述两个 JSON 文件(zh 与 en 键集一致)。

**ac:**
- given: en.json 与 zh-CN.json 存在
- when: 运行键一致性测试
- then: 两文件键集完全相等,且含 common.* 与 nav.* 前缀

**regression:**
- `npx vitest run src/stores/__tests__/preprocessing.test.ts`

**verify:**
- `npx vitest run src/i18n/i18n.test.ts`
- `npm run build`

---

### Task F3: Wire i18n into index.html, main.tsx and typed keys

**Files:**
- Modify: `web-ui/index.html`
- Modify: `web-ui/src/main.tsx`
- Create: `web-ui/src/i18n/types/i18next.d.ts`

**Interfaces:**
- Produces:
  - `index.html`: 主题内联脚本之后追加语言 FOUC 脚本(读 `pichost-locale`,无则按 `navigator.language` 前缀,设 `<html lang>`);`<html lang="en">` 初始值保留。
  - `main.tsx`: 顶部 `import './i18n'`。
  - `i18next.d.ts`: `CustomTypeOptions` 声明合并,`resources` 类型取自 en.json → `t('xxx')` 键编译期校验。
```ts
// src/i18n/types/i18next.d.ts
import en from '../locales/en.json'
declare module 'i18next' {
  interface CustomTypeOptions {
    resources: { translation: typeof en }
  }
}
```

**test_code**(先写,验证失败 — 类型级测试):
```ts
// src/i18n/i18n.test.ts 追加(编译期验证类型安全键)
it('typed t() accepts known key and rejects unknown (type-level)', () => {
  // 该断言在类型层验证: t('common.cancel') 通过编译;t('nope.missing') 编译失败
  expect(i18n.t('common.cancel')).toBe('Cancel')
})
```
并验证 `npm run build` 中 `tsc -b` 对 `t('不存在键')` 报错。

**impl_code**(最小实现):
按 Interfaces 描述修改 index.html(追加语言 FOUC 脚本)、main.tsx(`import './i18n'` 置于最顶部)、创建 i18next.d.ts。

**ac:**
- given: 页面加载(index.html 内联脚本)
- then: `<html lang>` 在 React 挂载前即为存储语言或浏览器语言
- given: `t('common.cancel')`
- then: 返回 "Cancel",且错误键在 `tsc -b` 时报编译错误

**regression:**
- `npx vitest run src/i18n/i18n.test.ts`
- `npm run build`(需先完成 F1/F2)

**verify:**
- `npx vitest run src/i18n/i18n.test.ts`
- `npm run build`

---

### Task F4: Add shared locale-aware format module

**Files:**
- Create: `web-ui/src/lib/format.ts`
- Create: `web-ui/src/hooks/useFormat.ts`
- Create: `web-ui/src/lib/format.test.ts`

**Interfaces:**
- Produces:
```ts
// src/lib/format.ts
export function formatBytes(bytes: number, locale: string): string   // B/KB/MB/GB/TB,Intl.NumberFormat 数字部分,index clamp,TB+ 不越界
export function formatDate(ts: number, locale: string): string        // Intl.DateTimeFormat(locale, {month:'short', day:'numeric', hour:'2-digit', minute:'2-digit'})
export function formatNumber(n: number, locale: string): string       // Intl.NumberFormat(locale).format(n)
// src/hooks/useFormat.ts
export function useFormat(): { formatBytes: (b: number) => string; formatDate: (t: number) => string; formatNumber: (n: number) => string }
// 内部从 i18n 取当前语言(useTranslation 订阅语言变化),绑定 locale 后返回
```

**test_code**(先写,验证失败):
```ts
// src/lib/format.test.ts
import { describe, it, expect } from 'vitest'
import { formatBytes, formatDate, formatNumber } from './format'

describe('formatBytes', () => {
  it('formats with units and clamps index', () => {
    expect(formatBytes(0, 'en')).toBe('0 B')
    expect(formatBytes(1536, 'en')).toBe('1.5 KB')
    expect(formatBytes(1024 ** 5, 'en')).toBe('1024 TB') // clamp 不越界
  })
  it('formats numbers locale-aware', () => {
    expect(formatNumber(1234567, 'en')).toContain(',')
  })
})
describe('formatDate', () => {
  it('renders without throwing for both locales', () => {
    expect(formatDate(0, 'en')).toBeTruthy()
    expect(formatDate(0, 'zh-CN')).toBeTruthy()
  })
})
```
Run: `npx vitest run src/lib/format.test.ts` — Expected: FAIL(模块不存在)。

**impl_code**(最小实现):
```ts
// src/lib/format.ts
const UNITS = ['B', 'KB', 'MB', 'GB', 'TB']
export function formatBytes(bytes: number, locale: string): string {
  if (!Number.isFinite(bytes) || bytes <= 0) return '0 B'
  const i = Math.min(Math.floor(Math.log(bytes) / Math.log(1024)), UNITS.length - 1)
  const value = bytes / 1024 ** i
  const digits = i === 0 ? 0 : 1
  return `${new Intl.NumberFormat(locale, { maximumFractionDigits: digits }).format(value)} ${UNITS[i]}`
}
export function formatDate(ts: number, locale: string): string {
  return new Intl.DateTimeFormat(locale, {
    month: 'short', day: 'numeric', hour: '2-digit', minute: '2-digit',
  }).format(new Date(ts))
}
export function formatNumber(n: number, locale: string): string {
  return new Intl.NumberFormat(locale).format(n)
}
```
```ts
// src/hooks/useFormat.ts
import { useTranslation } from 'react-i18next'
import { formatBytes, formatDate, formatNumber } from '../lib/format'
export function useFormat() {
  const { i18n } = useTranslation()
  const locale = i18n.language
  return {
    formatBytes: (b: number) => formatBytes(b, locale),
    formatDate: (t: number) => formatDate(t, locale),
    formatNumber: (n: number) => formatNumber(n, locale),
  }
}
```

**ac:**
- given: `formatBytes(1536, 'en')`
- then: 返回 '1.5 KB';`formatBytes(1024**5, 'en')` 不越界(TB clamp)
- given: `useFormat()` 在组件内使用
- then: 返回绑定当前 i18n.language 的三个函数

**regression:**
- `npx vitest run src/i18n/i18n.test.ts`

**verify:**
- `npx vitest run src/lib/format.test.ts`
- `npm run build`

---

### Task F5: Add LanguageSwitcher and extract Login/Register strings

**Files:**
- Create: `web-ui/src/components/LanguageSwitcher.tsx`
- Modify: `web-ui/src/pages/Login.tsx`
- Modify: `web-ui/src/pages/Register.tsx`
- *(同步更新 locales/en.json + zh-CN.json,并在 i18n.test.ts 维护键一致性 — 全局约束)*

**Interfaces:**
- Produces: `LanguageSwitcher` 组件(globe 图标 + 下拉: English / 简体中文,当前语言打勾,复用 `ui/DropdownMenu` 模式;点击调用 `i18n.changeLanguage(lng)`,持久化由 detector 完成)。
- Produces: `login.*` / `register.*` 键(~10 / ~12 条): `login.title`("Self-hosted image hosting")、`login.username`、`login.password`、`login.signIn`、`login.noAccount`、`login.register`、`login.loggedIn`、`register.createAccount`、`register.inviteCode`、`register.invitePlaceholder`、`register.adminCreated`、`register.registered` 等 — en 值 = 现有字面量,zh 翻译。

**test_code**(先写,验证失败 — 追加到 i18n.test.ts):
```ts
it('login and register prefixes exist in both locales', async () => {
  const en = (await import('./locales/en.json')).default as Record<string, unknown>
  const zh = (await import('./locales/zh-CN.json')).default as Record<string, unknown>
  for (const prefix of ['login', 'register']) {
    expect(Object.keys(en)).toContain(prefix)
    expect(Object.keys(zh)).toContain(prefix)
  }
})
```
Run: `npx vitest run src/i18n/i18n.test.ts` — Expected: FAIL(prefix 缺失)。

**impl_code**(最小实现):
创建 `LanguageSwitcher.tsx`(用 lucide Globe 图标 + 自定义下拉,参照 DropdownMenu 样式;`t('languageSwitcher.english')` / `t('languageSwitcher.chinese')` 亦入目录);Login/Register 页面引入 `useTranslation()`,全部 JSX 文本/placeholder/toast 替换为 `t('login.*')` / `t('register.*')`;Login/Register 页右上角放置紧凑 LanguageSwitcher(未登录可切换)。

**ac:**
- given: 未登录访问 /login
- when: 点击右上角语言切换器选 简体中文
- then: 页面文案切换为中文,localStorage['pichost-locale'] 更新,`<html lang>` 变为 zh-CN

**regression:**
- `npx vitest run src/i18n/i18n.test.ts`

**verify:**
- `npx vitest run src/i18n/i18n.test.ts`
- `npm run build`

---

### Task F6: Extract NavBar and shared toolbar strings

**Files:**
- Modify: `web-ui/src/components/NavBar.tsx`
- Modify: `web-ui/src/components/SearchBar.tsx`
- Modify: `web-ui/src/components/SortDropdown.tsx`
- *(同步更新 locales + i18n.test.ts)*

**Interfaces:**
- Produces: `nav.*`(~7 条: dashboard/gallery/admin/settings/logout/adminBadge)、`search.*`(searchPlaceholder "Search by filename…"、clearAria)、`sort.*`(uploadDate/fileSize/filename/sortAria/orderAria)、`themeToggle.*`(title)。
- NavBar 集成 `LanguageSwitcher`(ThemeToggle 旁)。

**test_code**(先写,验证失败 — i18n.test.ts 追加):
```ts
it('nav, search, sort and themeToggle prefixes exist in both locales', async () => {
  const en = (await import('./locales/en.json')).default as Record<string, unknown>
  const zh = (await import('./locales/zh-CN.json')).default as Record<string, unknown>
  for (const prefix of ['nav', 'search', 'sort', 'themeToggle']) {
    expect(Object.keys(en)).toContain(prefix)
    expect(Object.keys(zh)).toContain(prefix)
  }
})
```

**impl_code**(最小实现):
三个组件引入 `useTranslation()`,全部文本/aria/placeholder 替换为对应 `t()` 键;NavBar 用户下拉(Settings/Admin/Logout)文案用 `nav.*`;NavBar 放置 `LanguageSwitcher`;目录同步追加(zh 翻译)。

**ac:**
- given: NavBar 渲染
- then: 导航文案来自 t();语言切换器可见于 ThemeToggle 旁,切换后导航文案即时变化

**regression:**
- `npx vitest run src/i18n/i18n.test.ts`

**verify:**
- `npx vitest run src/i18n/i18n.test.ts`
- `npm run build`

---

### Task F7: Extract Dashboard, DropZone and UploadCard strings

**Files:**
- Modify: `web-ui/src/pages/Dashboard.tsx`
- Modify: `web-ui/src/components/DropZone.tsx`
- Modify: `web-ui/src/components/UploadCard.tsx`
- *(同步更新 locales + i18n.test.ts)*

**Interfaces:**
- Produces: `dashboard.*`(~17: adminBanner/goToAdmin/storageBackend/addSecondBackend/uploads/active/clearDone/storage/recent/detail/noImagesYet/...)、`dropzone.*`(~3)、`upload.*`(~9: queued/processing/uploading/uploaded/failed/open/copyUrl/copyMd/kb)。
- 顺带: Dashboard 内联 `(bytes/1024).toFixed(1) KB` 与 UploadCard 内联 KB → 改用 `useFormat().formatBytes`。

**test_code**(先写,验证失败 — i18n.test.ts 追加):
```ts
it('dashboard, dropzone and upload prefixes exist in both locales', async () => {
  const en = (await import('./locales/en.json')).default as Record<string, unknown>
  const zh = (await import('./locales/zh-CN.json')).default as Record<string, unknown>
  for (const prefix of ['dashboard', 'dropzone', 'upload']) {
    expect(Object.keys(en)).toContain(prefix)
    expect(Object.keys(zh)).toContain(prefix)
  }
})
```

**impl_code**(最小实现):
三组件引入 `useTranslation()` + `useFormat()`;全部文本/toast/placeholder 替换为 t() 键;格式化替换为 useFormat;目录同步追加(zh 翻译)。

**ac:**
- given: Dashboard 渲染
- then: 上传区/统计/最近图片文案来自 t();文件大小用 formatBytes(locale)
- given: 语言切换
- then: Dashboard 文案即时更新

**regression:**
- `npx vitest run src/i18n/i18n.test.ts`

**verify:**
- `npx vitest run src/i18n/i18n.test.ts`
- `npm run build`

---

### Task F8: Extract Gallery, UrlUploadInput and LinkCard strings

**Files:**
- Modify: `web-ui/src/pages/Gallery.tsx`
- Modify: `web-ui/src/components/UrlUploadInput.tsx`
- Modify: `web-ui/src/components/LinkCard.tsx`
- *(同步更新 locales + i18n.test.ts)*

**Interfaces:**
- Produces: `gallery.*`(~24: title/imagesCount/allBackends/selected/deselectAll/selectAll/delete/moveToCategory/loading/failedToLoad/noImagesFound/tryDifferentSearch/selectAria/loadingMore/allLoaded/deleteConfirm_one+_other/cancel/deleting)、`urlUpload.*`(~2)、`linkCard.*`(~3: copied/copyFailed/copyTitle)。
- 复数用 `_one`/`_other`(i18next 内建): `gallery.deleteConfirm_one` / `gallery.deleteConfirm_other`。

**test_code**(先写,验证失败 — i18n.test.ts 追加):
```ts
it('gallery, urlUpload and linkCard prefixes exist in both locales', async () => {
  const en = (await import('./locales/en.json')).default as Record<string, unknown>
  const zh = (await import('./locales/zh-CN.json')).default as Record<string, unknown>
  for (const prefix of ['gallery', 'urlUpload', 'linkCard']) {
    expect(Object.keys(en)).toContain(prefix)
    expect(Object.keys(zh)).toContain(prefix)
  }
})
it('gallery deleteConfirm plural keys present', async () => {
  const en = (await import('./locales/en.json')).default as any
  expect(en.gallery.deleteConfirm_one).toBeTruthy()
  expect(en.gallery.deleteConfirm_other).toBeTruthy()
})
```

**impl_code**(最小实现):
三组件引入 `useTranslation()`;全部文本/aria/confirm/toast 替换为 t() 键;复数键 `t('gallery.deleteConfirm', { count })`;`"N images"` 计数文案用复数+插值;目录同步追加(zh 复数形式与单数相同)。

**ac:**
- given: Gallery 有 3 张图
- then: 计数显示 "3 images"(复数),删除确认显示 "Delete 3 image(s)?" 的 `_other` 形式
- given: 切换 zh-CN
- then: 文案变为中文,复数无额外差异

**regression:**
- `npx vitest run src/i18n/i18n.test.ts`

**verify:**
- `npx vitest run src/i18n/i18n.test.ts`
- `npm run build`

---

### Task F9: Extract ImageDetail and Settings strings

**Files:**
- Modify: `web-ui/src/pages/ImageDetail.tsx`
- Modify: `web-ui/src/pages/Settings.tsx`
- *(同步更新 locales + i18n.test.ts)*

**Interfaces:**
- Produces: `imageDetail.*`(~30: back/name/saving/status/dimensions/type/size/uploaded/category/none/updating/generatedAssets/thumbnailUrl/webpUrl/links/linkFormat/url/markdown/html/bbcode/deleteImage/confirmDelete/cancel/deleting/imageDeleted/renameFailed/deleteFailed/loading/notFound)、`settings.*`(~28: 7 个分区标题 profile/password/storageUsage/storageBackends/watermark/preprocessing/oauth + 表单/toast 文案)。
- ImageDetail 内联 KB → `useFormat().formatBytes`;`toLocaleString()` 日期 → `formatDate`。

**test_code**(先写,验证失败 — i18n.test.ts 追加):
```ts
it('imageDetail and settings prefixes exist in both locales', async () => {
  const en = (await import('./locales/en.json')).default as Record<string, unknown>
  const zh = (await import('./locales/zh-CN.json')).default as Record<string, unknown>
  for (const prefix of ['imageDetail', 'settings']) {
    expect(Object.keys(en)).toContain(prefix)
    expect(Object.keys(zh)).toContain(prefix)
  }
})
```

**impl_code**(最小实现):
两组件引入 `useTranslation()` + `useFormat()`;全部文本/placeholder/toast/aria 替换为 t() 键;Settings 的 SECTION_META 标题数组改为 t() 键引用;日期/大小格式化替换;目录同步追加(zh 翻译)。

**ac:**
- given: ImageDetail 渲染
- then: 属性标签/链接格式/删除确认文案来自 t();日期用 locale 格式
- given: 语言切换
- then: ImageDetail 与 Settings 文案即时更新

**regression:**
- `npx vitest run src/i18n/i18n.test.ts`

**verify:**
- `npx vitest run src/i18n/i18n.test.ts`
- `npm run build`

---

### Task F10: Extract CategoryTree and StorageConfigSection strings

**Files:**
- Modify: `web-ui/src/components/CategoryTree.tsx`
- Modify: `web-ui/src/components/StorageConfigSection.tsx`
- *(同步更新 locales + i18n.test.ts)*

**Interfaces:**
- Produces: `categoryTree.*`(~18: categories/allImages/loading/noCategories/createOne/rename/delete/newCategory/categoryName/subCategoryNote/cancel/creating/create/deleteCategory/deleteConfirm/deleting)、`storageConfig.*`(~35: 全 CRUD 表单标签/校验错误 nameRequired/删除确认/8 个 toast/默认徽标)。
- 验证错误如 "Name is required" → `t('storageConfig.nameRequired')`。

**test_code**(先写,验证失败 — i18n.test.ts 追加):
```ts
it('categoryTree and storageConfig prefixes exist in both locales', async () => {
  const en = (await import('./locales/en.json')).default as Record<string, unknown>
  const zh = (await import('./locales/zh-CN.json')).default as Record<string, unknown>
  for (const prefix of ['categoryTree', 'storageConfig']) {
    expect(Object.keys(en)).toContain(prefix)
    expect(Object.keys(zh)).toContain(prefix)
  }
})
```

**impl_code**(最小实现):
两组件引入 `useTranslation()`;全部文本/aria/title/toast/校验错误替换为 t() 键;目录同步追加(zh 翻译)。

**ac:**
- given: CategoryTree 渲染
- then: 右键菜单(重命名/删除)、新建对话框、删除确认文案来自 t()
- given: 语言切换
- then: 组件文案即时更新

**regression:**
- `npx vitest run src/i18n/i18n.test.ts`

**verify:**
- `npx vitest run src/i18n/i18n.test.ts`
- `npm run build`

---

### Task F11: Extract SystemConfig, Watermark and Preprocessing strings

**Files:**
- Modify: `web-ui/src/components/SystemConfig.tsx`
- Modify: `web-ui/src/components/WatermarkSettings.tsx`
- Modify: `web-ui/src/components/PreprocessingSettings.tsx`
- *(同步更新 locales + i18n.test.ts;PreprocessingStatus.tsx 一并处理)*

**Interfaces:**
- Produces: `systemConfig.*`(~32: 卡片标题 database/redis/server/security/backups、testConnection、connectionOk、字段标签、backupCount、restore、unsavedChanges、saveRestart、backendHint 长文案、toasts)、`watermark.*`(~16)、`preprocessing.*`(~13 + status ~6)。

**test_code**(先写,验证失败 — i18n.test.ts 追加):
```ts
it('systemConfig, watermark and preprocessing prefixes exist in both locales', async () => {
  const en = (await import('./locales/en.json')).default as Record<string, unknown>
  const zh = (await import('./locales/zh-CN.json')).default as Record<string, unknown>
  for (const prefix of ['systemConfig', 'watermark', 'preprocessing']) {
    expect(Object.keys(en)).toContain(prefix)
    expect(Object.keys(zh)).toContain(prefix)
  }
})
```

**impl_code**(最小实现):
三个组件(含 PreprocessingStatus)引入 `useTranslation()`;全部文本/placeholder/toast/长文案替换为 t() 键;目录同步追加(zh 翻译)。

**ac:**
- given: SystemConfig 渲染
- then: 各分区标题/测试按钮/toast 文案来自 t()
- given: 语言切换
- then: 文案即时更新

**regression:**
- `npx vitest run src/i18n/i18n.test.ts`

**verify:**
- `npx vitest run src/i18n/i18n.test.ts`
- `npm run build`

---

### Task F12: Extract Admin stats/users/dialog strings

**Files:**
- Modify: `web-ui/src/pages/admin/AdminStats.tsx`
- Modify: `web-ui/src/pages/admin/AdminUsers.tsx`
- Modify: `web-ui/src/components/EditUserDialog.tsx`
- *(同步更新 locales + i18n.test.ts)*

**Interfaces:**
- Produces: `adminStats.*`(~9: totalUsers/totalImages/totalStorage/active24h/loading/storageBreakdown)、`adminUsers.*`(~13: 表头/管理员徽标/unlimited/删除确认/toasts)、`editUser.*`(~13: 表单标签/quota/unlimited/adminPrivileges/toasts)。
- AdminStats `toLocaleString()` → `useFormat().formatNumber`;AdminUsers `formatBytes` 重复实现 → `useFormat().formatBytes`。

**test_code**(先写,验证失败 — i18n.test.ts 追加):
```ts
it('adminStats, adminUsers and editUser prefixes exist in both locales', async () => {
  const en = (await import('./locales/en.json')).default as Record<string, unknown>
  const zh = (await import('./locales/zh-CN.json')).default as Record<string, unknown>
  for (const prefix of ['adminStats', 'adminUsers', 'editUser']) {
    expect(Object.keys(en)).toContain(prefix)
    expect(Object.keys(zh)).toContain(prefix)
  }
})
```

**impl_code**(最小实现):
三组件引入 `useTranslation()` + `useFormat()`;文本/toast/confirm 替换;格式化替换;目录同步追加(zh 翻译)。

**ac:**
- given: AdminStats 渲染
- then: 统计卡片与后端分解文案来自 t(),数字用 formatNumber
- given: 语言切换
- then: Admin 页面文案即时更新

**regression:**
- `npx vitest run src/i18n/i18n.test.ts`

**verify:**
- `npx vitest run src/i18n/i18n.test.ts`
- `npm run build`

---

### Task F13: Extract AdminInvites, CreateInviteDialog and Admin tabs

**Files:**
- Modify: `web-ui/src/pages/admin/AdminInvites.tsx`
- Modify: `web-ui/src/components/CreateInviteDialog.tsx`
- Modify: `web-ui/src/pages/Admin.tsx`
- *(同步更新 locales + i18n.test.ts)*

**Interfaces:**
- Produces: `adminInvites.*`(~20: 状态标签 used/expired/active、timeRemaining 插值 "{{days}}d {{hours}}h remaining"、表头、createCode、emptyState、copyToasts)、`createInvite.*`(~13)、`adminTabs.*`(overview/users/invites/systemConfig + title)。
- AdminInvites 硬编码 `'en-US'` 日期 → `useFormat().formatDate`;`timeRemaining` 单位文案改 t() + 插值。

**test_code**(先写,验证失败 — i18n.test.ts 追加):
```ts
it('adminInvites, createInvite and adminTabs prefixes exist in both locales', async () => {
  const en = (await import('./locales/en.json')).default as Record<string, unknown>
  const zh = (await import('./locales/zh-CN.json')).default as Record<string, unknown>
  for (const prefix of ['adminInvites', 'createInvite', 'adminTabs']) {
    expect(Object.keys(en)).toContain(prefix)
    expect(Object.keys(zh)).toContain(prefix)
  }
})
```

**impl_code**(最小实现):
三组件引入 `useTranslation()` + `useFormat()`;文本/toast/日期/时间剩余替换;目录同步追加(zh 翻译)。

**ac:**
- given: AdminInvites 渲染
- then: 状态/表头/创建对话框文案来自 t(),日期用 locale 格式
- given: 语言切换
- then: 文案即时更新

**regression:**
- `npx vitest run src/i18n/i18n.test.ts`

**verify:**
- `npx vitest run src/i18n/i18n.test.ts`
- `npm run build`

---

### Task F14: Add ApiError parsing, Accept-Language hook and behavior codes

**Files:**
- Modify: `web-ui/src/api/client.ts`
- Create: `web-ui/src/api/errors.ts`
- Create: `web-ui/src/api/errors.test.ts`

**Interfaces:**
- Produces:
```ts
// src/api/errors.ts
export interface ApiError { status: number; code: string; message: string }
export function getErrorCode(err: unknown): string | null          // HTTPError → 解析 body.code
export function isErrorCode(err: unknown, code: string): boolean   // 行为判断辅助
// client.ts 改造:
//  - beforeRequest hook 追加: request.headers.set('Accept-Language', getCurrentLocale())
//  - beforeError hook: 解析 error.response JSON body → error.message = body.error(本地化消息);
//    同时挂载 body.code 到错误对象(如 (error as any).code = body.code)
//  - HTTPError 透传,组件现有 toast.error(err.message) 无需改动即显示本地化消息
```

**test_code**(先写,验证失败):
```ts
// src/api/errors.test.ts
import { describe, it, expect, vi } from 'vitest'
import { getErrorCode, isErrorCode } from './errors'

function makeError(code: string | null, status = 400): unknown {
  const err = new Error('HTTP Error') as any
  err.response = { status, json: async () => ({ error: 'msg', code }) }
  return err
}
describe('getErrorCode', () => {
  it('extracts code from HTTPError body', async () => {
    expect(await getErrorCode(makeError('auth.invalid_token'))).toBe('auth.invalid_token')
    expect(await getErrorCode(makeError(null))).toBeNull()
    expect(await getErrorCode(new Error('plain'))).toBeNull()
  })
})
describe('isErrorCode', () => {
  it('matches behavior codes', async () => {
    expect(await isErrorCode(makeError('auth.invalid_token'), 'auth.invalid_token')).toBe(true)
    expect(await isErrorCode(makeError('other'), 'auth.invalid_token')).toBe(false)
  })
})
```
Run: `npx vitest run src/api/errors.test.ts` — Expected: FAIL(模块不存在)。

**impl_code**(最小实现):
按 Interfaces 实现 errors.ts 与 client.ts 改造(ky `beforeRequest` + `beforeError` hooks;`beforeError` 中 `await error.response.clone().json()` 解析并改写 message)。行为码消费点(实现计划外的最小接缝,随用随加): `isErrorCode(err, 'auth.invalid_token')` → 触发 `forceLogout()`(可并入现有 401 拦截逻辑)。

**ac:**
- given: 后端返回 `{"error":"用户名或密码错误","code":"auth.invalid_credentials"}`
- when: ky 请求失败抛错
- then: `err.message` 为本地化消息;`getErrorCode(err)` 返回 code
- given: 任何请求发出
- then: 请求头携带 `Accept-Language: <当前 UI 语言>`

**regression:**
- `npx vitest run src/i18n/i18n.test.ts`
- `npx vitest run src/stores/__tests__/preprocessing.test.ts`

**verify:**
- `npx vitest run src/api/errors.test.ts`
- `npm run build`

---

### Task F15: Add Language field to SystemConfig UI

**Files:**
- Modify: `web-ui/src/components/SystemConfig.tsx`
- Modify: `web-ui/src/api/client.ts`(config 类型增加 `i18n` 字段)

**Interfaces:**
- Consumes: 后端 T12 的 `SystemConfig.i18n`(GET/PUT /admin/config 携带 `i18n.language`)
- Produces: SystemConfig 新增 "Localization" 分区(或并入 Security): 语言下拉(English / 简体中文),保存后 PUT `i18n.language`;提示"语言变更即时生效"(后端已 reload,无需重启)。

**test_code**(先写,验证失败 — 类型级 + 键断言):
```ts
// i18n.test.ts 追加
it('systemConfig language labels exist', async () => {
  const en = (await import('./locales/en.json')).default as any
  expect(en.systemConfig.language).toBeTruthy()
})
```

**impl_code**(最小实现):
SystemConfig.tsx: 新增语言选择控件(复用现有表单模式),保存 payload 含 `i18n: { language }`;client.ts config 类型补 `i18n?: { language?: string; locales_dir?: string | null }`;目录追加 `systemConfig.language` / `systemConfig.languageHint` 键(zh 翻译)。

**ac:**
- given: admin 在 SystemConfig 选择 简体中文 并保存
- then: PUT /admin/config 携带 `i18n.language=zh-CN`,后续 API 错误消息立即为中文(无需重启)

**regression:**
- `npx vitest run src/i18n/i18n.test.ts`
- `npm run build`

**verify:**
- `npx vitest run src/i18n/i18n.test.ts`
- `npm run build`

---

### Task F16: Bump version and sync docs

**Files:**
- Modify: `Cargo.toml`(workspace version 0.17.5 → 0.18.0)
- Modify: `web-ui/package.json`(version 同步 0.18.0)
- Modify: `AGENTS.md`(i18n 架构、新配置项 `PICHOST_I18N_LANGUAGE` / `PICHOST_I18N_LOCALES_DIR`、错误信封 code 字段、迁移数不变、新增 API 无)

**Interfaces:**
- 无新接口;纯版本与文档同步。

**test_code**(先写,验证失败 — 无测试,验证为文档/版本一致性检查):
```bash
grep -n 'version' Cargo.toml web-ui/package.json | head -20
```

**impl_code**(最小实现):
- 版本 bump: workspace `Cargo.toml` 与 `web-ui/package.json` 0.17.5 → 0.18.0
- `AGENTS.md` 同步: 错误信封格式(含 code)、`PICHOST_I18N_LANGUAGE` / `PICHOST_I18N_LOCALES_DIR` 配置、i18n 模块说明、locale 目录结构
- `README.md` 同步: 配置表新增两个变量、国际化特性条目
- `.omo/summary/summary_and_next.md` 新增 "## i18n 国际化 ✅ (本次完成)" 章节,更新待实施表
- 三文件一起提交: `docs: auto-sync AGENTS.md, README.md, summary after i18n completion`(README/summary 与 AGENTS.md 可分两次提交,消息风格一致)

**ac:**
- given: 全部 T0-T15/F1-F15 完成且质量门通过
- then: 版本为 0.18.0;AGENTS.md/README.md/summary 反映 i18n 架构与配置

**regression:**
- `cargo test --workspace`
- `npm run build`

**verify:**
- `cargo test --workspace`
- `cargo clippy --workspace -- -D warnings`
- `npx vitest run`
- `npm run build`

---

## 验证清单(全部任务完成后)

```bash
cargo test --workspace                                  # 313+ 通过(无 infra)
cargo test --workspace -- --include-ignored             # 555+ 通过(Docker PG+Redis+MinIO)
cargo clippy --workspace -- -D warnings                 # 零警告
npx vitest run                                          # 前端单测全绿(i18n/format/errors/preprocessing)
npm run build                                           # tsc -b + vite build
```

- 手动验证: ① 浏览器 zh 环境访问登录页自动中文;② NavBar 切换语言即时生效且刷新保持;③ 错误响应含 code 字段且消息随 Accept-Language 变化;④ admin 在 SystemConfig 改语言后无需重启即生效;⑤ 外部 locales 目录文件修改后 ≤5s 生效(热更新)。

## 已知风险与回退

- 集成测试断言消息文本 → 已迁移为断言 code(T11);默认 en 下消息逐字不变,未迁移断言依然通过。
- `JsonBody` 替换 `Json` 是全局机械改动(各转换任务内完成);若某处遗漏导致 rejection 回归,用 `cargo test -p pichost-api --test auth_test -- --include-ignored` 定位。
- 语言键漂移 → `i18n.test.ts` 键一致性测试兜底;后端 key 缺失回退显示 key 本身,便于排查。



